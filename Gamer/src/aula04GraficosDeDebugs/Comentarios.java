package aula04GraficosDeDebugs;

import java.awt.Color;
import java.awt.Graphics;

public class Comentarios {
	
	/**Graphics g = image.getGraphics();
		g.setColor(new Color(19,19,19));
		g.fillRect(0, 0, WIDTH,HEIGHT);os 2 primeiros são a posição Y e X, e os 2 ultimo largura e altura
	a função G.FILLRECT serve para desenha RETANGULOS, sempre que REDENRIZAMOS temos que 
	execultar a linha 10 e 11, para zeramos a nossa tela.Pois precisamos apagar tudo que tinha
	ANTES no nosso caso com um retangulo da cor PRETA, para voltar a RENDERIZAR
	além disto podemos renderizar cada retangulo
		
		g = bs.getDrawGraphics();
		g.drawImage(image,0,0, WIDTH*SCALE, HEIGHT*SCALE, null);
		bs.show(); **/

}
